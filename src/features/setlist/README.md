# Set List feature modules

Phase I.1 begins Set List ownership extraction.

Current owner:

- `personal-set-controller.js` — personal set list shape, counts, item mutations, set CRUD primitives, and preservation contract for future set-entry fields.

Not owned here yet:

- Workspace set lists
- Add-to-set destination flow
- Settings cleanup
- PDF/export UI
- Future section-filtering UI
- Attached song PDF workflows, which remain cancelled
