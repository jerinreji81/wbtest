# Core source owners

Phase D active owners:

- `constants.js` — static constants and configuration defaults.
- `utils.js` — pure music-key helpers and safe localStorage wrappers.

Future phases should add service/controller source here only when the current owner is removed from the legacy shell in the same phase.

- `navigation.js` — Phase G owner for tab registry, route registry, navigation adapter, and raw edge-back gesture API.
